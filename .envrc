# direnv config (MIT) — auto-activates the repo's mise toolchain on cd.
#
# First-time:  direnv allow .

use mise

# Repo-local overrides — uncomment as needed:
# export DB_CONNECTION=pgsql
# export DATABASE_URL=postgresql://parkhub:parkhub@localhost:5433/parkhub
# export VITE_API_URL=http://127.0.0.1:8000
# export APP_URL=http://127.0.0.1:8000
